# Release 0.10.31

- Upgrade Alerts to the Calcit 0.14.11 toolchain (`@calcit/procs` 0.14.11) and align the provider chain to reel 0.6.20, respo-ui 0.7.20, respo-router 0.8.14, respo-markdown 0.4.34, Respo 0.16.97, and js-ffi 0.1.16.
- Replace originless struct/map/string methods and primitives (`&struct:get`, `assoc`, `merge`, `dissoc`, `get`, `update`, `?` optional params) with typed accessors, `&map:*`/`&merge`, and reviewed helpers.
- Declare `AlertsDom`/`AlertsDomStyle` external-object traits for the fade/scale/slide DOM effects, and type the reel atom through `reel.typed` (`record-op`/`refresh`) instead of the loose `reel.core` updater.
- Reviewed baseline update: `typeNotFull` 54 -> 55 for the added helpers/traits; `schemaDynamic` 77 -> 65, `codeNil` 48 -> 31, and `unresolved` 125 -> 96.
- Revalidated strict Caps, `--check-only` (zero warnings), the type/weak-type/deprecated analyses, the upgrade baseline, JS code generation, and the Vite production build.
