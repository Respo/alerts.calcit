# 升级类型声明到 Calcit 0.13.13

- 将 `%alert-actions.show` 的 `Optional String` 迁移为 `Option String`。
- 使用 `option:some?` 与 `option:unwrap-or` 处理 Option 文本，消除新版 nominal enum 告警。
- 同步 respo-ui 0.7.7；使用核心 Calcit 0.13.13 通过 check-only、测试与 JS 生成。
